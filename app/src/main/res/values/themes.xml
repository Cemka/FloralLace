<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.FloralLace" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@color/white</item> <!-- Цвет фона -->
        <item name="android:statusBarColor">@color/green</item> <!-- Цвет статус-бара -->
        <item name="android:navigationBarColor">@color/white</item> <!-- Цвет навигационной панели -->
        <item name="android:windowContentOverlay">@null</item>
    </style>
</resources>